let array = [1, 2, 3, 4, 5];

// Use reduce to calculate the sum of array elements

console.log(array.reduce((number, total) => number + total, 0));

Math.max(array);

/* 
// Use reduce to calculate the product of array elements

// Use reduce to find the maximum value in the array

// Use reduce to find the minimum value in the array

// Use reduce to count the occurrences of each element in the array
let array = ['a', 'b', 'a', 'c', 'b', 'a'];

// Use reduce to flatten the array of arrays
let array = [[1, 2], [3, 4], [5, 6]];

// Use reduce to remove duplicates from the array
let array = [1, 2, 2, 3, 4, 4, 5];

// Use reduce to group the objects by the 'age' property
let array = [{ age: 20 }, { age: 21 }, { age: 20 }];

*/
